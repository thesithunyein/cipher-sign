# After tee stack is up (see docs/SETUP.md):
# Tunnel / local external proxy port is 6674 (Flare FCC guide).
# Example: cloudflared tunnel --url http://localhost:6674

VITE_DIRECT_URL=http://127.0.0.1:6674
VITE_DIRECT_API_KEY=same-as-tee-DIRECT_API_KEY
