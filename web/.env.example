# After tee/scripts/full-setup.sh --test succeeds:
# Tunnel URL usually targets the external proxy port (see tee README).
# Example: cloudflared tunnel --url http://localhost:6676

VITE_DIRECT_URL=http://127.0.0.1:6676
VITE_DIRECT_API_KEY=same-as-tee-DIRECT_API_KEY
