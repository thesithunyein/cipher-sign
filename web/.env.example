# Local live TEE for screen record (Vite proxies /fcc → :6674, no CORS issues).
# After tee stack is up: npm run live:smoke && npm run dev

VITE_DIRECT_URL=/fcc
VITE_DIRECT_API_KEY=same-as-tee-DIRECT_API_KEY
